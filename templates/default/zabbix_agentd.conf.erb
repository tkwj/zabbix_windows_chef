Hostname=<%= node.zabbix_windows.agent.hostname %>
LogFile=<%= node.zabbix_windows.log_dir %>\zabbix_agentd.log
DebugLevel=2
EnableRemoteCommands=1
LogRemoteCommands=1
Include=<%= node.zabbix_windows.agent.include_dir %>
Server=<%= node.zabbix_windows.agent.servers.join(',') %>
<% if node.zabbix_windows.agent.servers_active.first -%>
ServerActive=<%= node.zabbix_windows.agent.servers_active.join(',') %>
<% end -%>
StartAgents=4
